#include <glad/glad.h>
#include <GLFW/glfw3.h>
#include <stdio.h>

int main() {
//    if (!glfwInit()) {
//        fprintf(stderr, "Failed to initialise GLFW\n");
//        return -1;
//    }
    printf("Hello World!\n");
    return 0;

    // OpenGL version 3.3 core profile
}
